apiVersion: v1
kind: Service
metadata:
  name: restaurant-service
  namespace: yelp-app
spec:
  type: ClusterIP
  selector:
    app: restaurant-service
  ports:
    - port: 8003
      targetPort: 8003
      protocol: TCP
