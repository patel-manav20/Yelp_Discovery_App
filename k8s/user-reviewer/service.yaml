apiVersion: v1
kind: Service
metadata:
  name: user-reviewer-service
  namespace: yelp-app
spec:
  type: ClusterIP
  selector:
    app: user-reviewer-service
  ports:
    - port: 8001
      targetPort: 8001
      protocol: TCP
